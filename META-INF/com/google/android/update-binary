#!/sbin/sh
API="$1"; MAGISK_VER="$2"; ZIP="$3"
TMPDIR=/dev/tmp/v4a_a15_auto
rm -rf "$TMPDIR"; mkdir -p "$TMPDIR"
unzip -o "$ZIP" -d "$TMPDIR" >/dev/null 2>&1
export ZIPDIR="$TMPDIR"
sh "$TMPDIR/customize.sh" "$@"
exit $?
